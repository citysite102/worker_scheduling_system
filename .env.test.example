# ============================================================
# 測試環境變數範本（.env.test.example）
# ============================================================
# 使用方式：
#   1. 複製此檔案為 .env.test
#   2. 填入測試專用的資料庫連線字串
#   3. .env.test 已加入 .gitignore，不會被提交到 Git
#
# 重要原則：
#   TEST_DATABASE_URL 必須指向「測試專用資料庫」，
#   絕對不能與正式環境的 DATABASE_URL 相同。
#   測試執行時會寫入並清除資料，若誤用正式資料庫將造成資料損失。
# ============================================================

# 測試專用資料庫連線（必填）
# 格式：mysql://用戶名:密碼@主機:埠號/資料庫名稱
# 範例（本地測試資料庫）：
# TEST_DATABASE_URL=mysql://root:password@localhost:3306/worker_scheduling_test
#
# 範例（Manus 測試環境，使用與正式環境不同的 schema）：
# TEST_DATABASE_URL=mysql://user:pass@host:3306/worker_scheduling_dev_test
TEST_DATABASE_URL=

# 測試環境標記（由 vitest.config.ts 自動注入，通常不需手動設定）
# VITEST=true
